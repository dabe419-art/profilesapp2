"use strict";
var __getOwnPropNames = Object.getOwnPropertyNames;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};

// ../private-tools/lib/subprocess/index.js
var require_subprocess = __commonJS({
  "../private-tools/lib/subprocess/index.js"(exports2) {
    "use strict";
    var __createBinding2 = exports2 && exports2.__createBinding || (Object.create ? (function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    }));
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    }) : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || /* @__PURE__ */ (function() {
      var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function(o2) {
          var ar = [];
          for (var k in o2) if (Object.prototype.hasOwnProperty.call(o2, k)) ar[ar.length] = k;
          return ar;
        };
        return ownKeys(o);
      };
      return function(mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) {
          for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding2(result, mod, k[i]);
        }
        __setModuleDefault(result, mod);
        return result;
      };
    })();
    var __importDefault = exports2 && exports2.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.SubprocessError = void 0;
    exports2.run = run;
    exports2.runSync = runSync;
    exports2.runUserCommandLine = runUserCommandLine;
    exports2.renderForDisplay = renderForDisplay;
    var child_process = __importStar(require("child_process"));
    var string_decoder_1 = require("string_decoder");
    var cross_spawn_1 = __importDefault(require("cross-spawn"));
    var SubprocessError = class extends Error {
      code = "SUBPROCESS_FAILED";
      kind;
      command;
      exitCode;
      signal;
      stdout;
      stderr;
      constructor(props) {
        const kind = props.exitCode != null ? "exited" : props.signal != null ? "killed" : "spawn-failed";
        super(subprocessErrorMessage(kind, props), props.cause !== void 0 ? { cause: props.cause } : void 0);
        this.kind = kind;
        this.command = props.command;
        this.exitCode = props.exitCode;
        this.signal = props.signal;
        this.stdout = props.stdout;
        this.stderr = props.stderr;
      }
    };
    exports2.SubprocessError = SubprocessError;
    function subprocessErrorMessage(kind, props) {
      switch (kind) {
        case "exited":
          return `${props.command} exited with error code ${props.exitCode}`;
        case "killed":
          return `${props.command} exited with signal ${props.signal}`;
        case "spawn-failed":
          return `${props.command} failed to start: ${errorMessage(props.cause)}`;
      }
    }
    function errorMessage(cause) {
      if (typeof cause === "object" && cause !== null && "message" in cause) {
        return String(cause.message);
      }
      return String(cause);
    }
    async function run(argv, options = {}) {
      assertNonEmptyArgv(argv, "run");
      const child = (0, cross_spawn_1.default)(argv[0], argv.slice(1), spawnOptions(options));
      return monitor(child, renderForDisplay(argv), options);
    }
    function runSync(argv, options = {}) {
      assertNonEmptyArgv(argv, "runSync");
      const result = cross_spawn_1.default.sync(argv[0], argv.slice(1), {
        cwd: options.cwd,
        timeout: options.timeoutMs,
        killSignal: "SIGTERM",
        encoding: "utf-8",
        windowsHide: true,
        stdio: ["ignore", "pipe", "ignore"]
      });
      if (result.status === 0) {
        return result.stdout;
      }
      throw new SubprocessError({
        command: renderForDisplay(argv),
        exitCode: result.status,
        signal: result.signal,
        stdout: result.stdout ?? "",
        stderr: "",
        cause: result.error
      });
    }
    async function runUserCommandLine(commandLine, options = {}) {
      const child = child_process.spawn(commandLine, {
        ...spawnOptions(options),
        // eslint-disable-next-line no-restricted-syntax -- this is the single sanctioned shell entry point (see the module header)
        shell: true
      });
      return monitor(child, commandLine, options);
    }
    function renderForDisplay(argv, platform = process.platform) {
      return platform === "win32" ? argv.map(windowsDisplayEscape).join(" ") : argv.map(posixDisplayEscape).join(" ");
    }
    var DISPLAY_SAFE = /^[A-Za-z0-9_%+=:,.@/-]+$/;
    function posixDisplayEscape(x) {
      if (DISPLAY_SAFE.test(x)) {
        return x;
      }
      return `'${x.replace(/'/g, `'"'"'`)}'`;
    }
    function windowsDisplayEscape(x) {
      if (DISPLAY_SAFE.test(x.replace(/\\/g, "/")) && !x.includes("%")) {
        return x;
      }
      let escaped = x.replace(/(\\*)"/g, '$1$1\\"');
      escaped = escaped.replace(/(\\+)$/, "$1$1");
      return `"${escaped}"`;
    }
    function assertNonEmptyArgv(argv, fn) {
      if (argv.length === 0 || !argv[0]) {
        throw new Error(`${fn}() requires a non-empty argv`);
      }
    }
    function spawnOptions(options) {
      const stdio = options.stdio ?? "capture";
      const stdin = options.input != null ? "pipe" : "ignore";
      return {
        cwd: options.cwd,
        env: options.env,
        timeout: options.timeoutMs,
        killSignal: "SIGTERM",
        // Do not flash a console window when the CLI is invoked from a GUI
        // context on Windows (exec() used to default to this; spawn() does not).
        windowsHide: true,
        stdio: [
          stdin,
          stdio === "inherit" ? "inherit" : "pipe",
          stdio === "capture" ? "pipe" : "inherit"
        ]
      };
    }
    function monitor(child, displayCommand, options) {
      return new Promise((resolve, reject) => {
        const collect = options.collect ?? true;
        const stdout = new Array();
        const stderr = new Array();
        let settled = false;
        const emit = options.onOutput ?? (() => {
        });
        const lines = options.buffering === "lines" ? lineBuffer(emit) : void 0;
        const settle = (fn) => {
          if (!settled) {
            settled = true;
            fn();
          }
        };
        if (options.input != null) {
          child.stdin.on("error", () => {
          });
          child.stdin.write(options.input);
          child.stdin.end();
        }
        const attach = (stream, name, into) => {
          if (!stream) {
            return;
          }
          const decoder = new string_decoder_1.StringDecoder("utf-8");
          stream.on("data", (chunk) => {
            const data = decoder.write(chunk);
            if (!data) {
              return;
            }
            if (collect) {
              into.push(data);
            }
            lines ? lines.write(name, data) : emit(name, data);
          });
          stream.on("end", () => {
            const rest = decoder.end();
            if (!rest) {
              return;
            }
            if (collect) {
              into.push(rest);
            }
            lines ? lines.write(name, rest) : emit(name, rest);
          });
        };
        attach(child.stdout, "stdout", stdout);
        attach(child.stderr, "stderr", stderr);
        child.once("error", (cause) => {
          settle(() => reject(new SubprocessError({
            command: displayCommand,
            exitCode: null,
            signal: null,
            stdout: stdout.join(""),
            stderr: stderr.join(""),
            cause
          })));
        });
        child.once("close", (exitCode, signal) => {
          lines?.flush();
          settle(() => {
            if (exitCode === 0) {
              resolve({ stdout: stdout.join(""), stderr: stderr.join("") });
            } else {
              reject(new SubprocessError({
                command: displayCommand,
                exitCode,
                signal,
                stdout: stdout.join(""),
                stderr: stderr.join("")
              }));
            }
          });
        });
      });
    }
    function lineBuffer(emit) {
      const residue = { stdout: [], stderr: [] };
      return {
        write(stream, data) {
          if (!data.includes("\n")) {
            residue[stream].push(data);
            return;
          }
          const parts = (residue[stream].join("") + data).split(/\r?\n/);
          residue[stream] = [parts.pop()];
          for (const line of parts) {
            emit(stream, line);
          }
        },
        flush() {
          for (const stream of ["stdout", "stderr"]) {
            const rest = residue[stream].join("");
            if (rest) {
              emit(stream, rest);
              residue[stream] = [];
            }
          }
        }
      };
    }
  }
});

// ../private-tools/lib/zip/index.js
var require_zip = __commonJS({
  "../private-tools/lib/zip/index.js"(exports2) {
    "use strict";
    var __createBinding2 = exports2 && exports2.__createBinding || (Object.create ? (function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    }));
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    }) : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || /* @__PURE__ */ (function() {
      var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function(o2) {
          var ar = [];
          for (var k in o2) if (Object.prototype.hasOwnProperty.call(o2, k)) ar[ar.length] = k;
          return ar;
        };
        return ownKeys(o);
      };
      return function(mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) {
          for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding2(result, mod, k[i]);
        }
        __setModuleDefault(result, mod);
        return result;
      };
    })();
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.zipDirectory = zipDirectory;
    exports2.zipString = zipString;
    var fs_1 = require("fs");
    var path = __importStar(require("path"));
    var fast_glob_1 = require("fast-glob");
    var yazl_1 = require("yazl");
    function epoch() {
      return new Date(1980, 0, 1, 0, 0, 0);
    }
    async function zipDirectory(directory, outputFile, eventEmitter = () => {
    }) {
      const temporaryOutputFile = `${outputFile}.${randomString()}._tmp`;
      await writeZipFile(directory, temporaryOutputFile);
      await moveIntoPlace(temporaryOutputFile, outputFile, eventEmitter);
    }
    function zipString(fileName, rawString) {
      return new Promise((resolve, reject) => {
        const buffers = [];
        const zip = new yazl_1.ZipFile();
        zip.outputStream.on("data", (chunk) => buffers.push(chunk));
        zip.outputStream.on("error", reject);
        zip.outputStream.on("end", () => resolve(Buffer.concat(buffers)));
        zip.addBuffer(Buffer.from(rawString), fileName, {
          mtime: epoch(),
          // reset date to get the same hash for the same content
          // Only emit the DOS timestamp (no UTC "universal time" extended field),
          // so the archive bytes do not depend on the machine's timezone.
          forceDosTimestamp: true
        });
        zip.end();
      });
    }
    function writeZipFile(directory, outputFile) {
      return new Promise(async (ok, fail) => {
        const globOptions = {
          dot: true,
          onlyFiles: true,
          followSymbolicLinks: true,
          cwd: directory
        };
        const files = (0, fast_glob_1.globSync)("**", globOptions);
        const zip = new yazl_1.ZipFile();
        zip.outputStream.on("error", fail);
        const output = (0, fs_1.createWriteStream)(outputFile);
        output.on("error", fail);
        output.once("close", ok);
        zip.outputStream.pipe(output);
        for (const file of files) {
          const fullPath = path.resolve(directory, file);
          const [data, stat] = await Promise.all([fs_1.promises.readFile(fullPath), fs_1.promises.stat(fullPath)]);
          zip.addBuffer(data, file, {
            mtime: epoch(),
            // reset dates to get the same hash for the same content
            mode: stat.mode,
            // Only emit the DOS timestamp (no UTC "universal time" extended field),
            // so the archive bytes do not depend on the machine's timezone.
            forceDosTimestamp: true
          });
        }
        zip.end();
      });
    }
    async function moveIntoPlace(source, target, eventEmitter) {
      let delay = 100;
      let attempts = 5;
      while (true) {
        try {
          await fs_1.promises.rename(source, target);
          return;
        } catch (e) {
          if (e.code !== "EPERM" || attempts-- <= 0) {
            throw e;
          }
          eventEmitter(e.message);
          await sleep(Math.floor(Math.random() * delay));
          delay *= 2;
        }
      }
    }
    function sleep(ms) {
      return new Promise((ok) => setTimeout(ok, ms));
    }
    function randomString() {
      return Math.random().toString(36).replace(/[^a-z0-9]+/g, "");
    }
  }
});

// ../private-tools/lib/s3-path-style/index.js
var require_s3_path_style = __commonJS({
  "../private-tools/lib/s3-path-style/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.forceS3PathStyle = forceS3PathStyle;
    function forceS3PathStyle() {
      if (process.env.CDK_S3_FORCE_PATH_STYLE) {
        return true;
      }
      const endpoint = process.env.AWS_ENDPOINT_URL_S3 ?? process.env.AWS_ENDPOINT_URL;
      if (endpoint && isLoopbackEndpoint(endpoint)) {
        return true;
      }
      return void 0;
    }
    function isLoopbackEndpoint(endpoint) {
      let host;
      try {
        host = new URL(endpoint).hostname;
      } catch {
        return false;
      }
      const normalized = host.replace(/^\[|\]$/g, "").toLowerCase();
      return normalized === "localhost" || normalized.startsWith("127.") || normalized === "::1";
    }
  }
});

// lib/private/tools.js
var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
  if (k2 === void 0) k2 = k;
  var desc = Object.getOwnPropertyDescriptor(m, k);
  if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
    desc = { enumerable: true, get: function() {
      return m[k];
    } };
  }
  Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
  if (k2 === void 0) k2 = k;
  o[k2] = m[k];
}));
var __exportStar = exports && exports.__exportStar || function(m, exports2) {
  for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
__exportStar(require_subprocess(), exports);
__exportStar(require_zip(), exports);
__exportStar(require_s3_path_style(), exports);
