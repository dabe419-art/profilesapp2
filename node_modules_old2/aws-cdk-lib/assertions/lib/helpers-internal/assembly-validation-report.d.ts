import type { PolicyViolationSeverity } from '@aws-cdk/cloud-assembly-schema';
import type { App } from '../../../core/lib/app';
/**
 * An API to access and assert on the validation report of an application
 *
 * Currently still a helper API because I want to centralize the loading and asserting
 * in this codebase without necessarily committing to any public API yet.
 */
export declare class AssemblyValidationReport {
    private readonly report;
    static readonly APP_CONTEXT: {
        "@aws-cdk/core:failSynthOnValidationErrors": boolean;
        "@aws-cdk/core:strictCfnValidateErrors": boolean;
    };
    static fromApp(app: App): AssemblyValidationReport;
    private constructor();
    /**
     * All violations in all reports
     */
    allViolations(): import("@aws-cdk/cloud-assembly-schema").PolicyViolationJson[];
    hasViolation(pattern?: PartialViolation): void;
    hasNoViolation(pattern?: PartialViolation): void;
}
export interface PartialViolation {
    readonly ruleName?: any;
    readonly description?: any;
    readonly severity?: PolicyViolationSeverity | any;
}
