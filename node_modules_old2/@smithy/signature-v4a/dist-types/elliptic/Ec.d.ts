/**
 * Compatibility wrapper matching the elliptic.js Ec interface used by SignatureV4a.
 * Provides: new Ec("p256").keyFromPrivate(key).sign(hash).toDER()
 */
declare class Ec {
    constructor(curve: any);
    keyFromPrivate(privateKey: any): {
        sign(hash: any): {
            toDER(): any;
        };
    };
}
export { Ec };
